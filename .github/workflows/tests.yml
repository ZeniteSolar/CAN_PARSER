
name: tests

on: [push, "pull_request"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Compile tests
      run: |
        make tests
    - name: Run tests
      run: |
        ./tests
      